#include 
<stdio.h>
int main() 
{
    int sum = 0;
    float average;
    int count = 0;

    for (int i = 1; i <= 20; i++) {
        sum += i;
        count++;
    }

    average = sum / count;

    printf("Average of numbers between 1 to 20 is: %.2f\n", average);

    return 0;
}
